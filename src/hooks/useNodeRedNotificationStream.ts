/**
 * Charger notification polling and toasts live in Header.tsx (pollNotifications).
 * This hook is kept as a no-op so DashboardLayout does not need a separate subscriber.
 */
export function useNodeRedNotificationStream() {}
